#!/bin/bash

note_dir="${HOME}/notes"
selected_file="$(cd $note_dir && fd -t f | rofi -dmenu -p "File")"
if [ -z "$selected_file" ]; then
  echo "No file found"
  exit 1
fi

i3.dd.emacs '(find-file "'"${note_dir}/${selected_file}"'")'

# vim: ft=sh
