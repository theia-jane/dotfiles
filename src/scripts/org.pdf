#!/usr/bin/env emacs-eval

(if (not (eq (length argv) 0))
  (progn 
    (find-file (car argv))
    (org-latex-export-to-pdf)
    (delete-file (replace-regexp-in-string "org$" "tex" (car argv)))))

;; vim: ft=lisp  
