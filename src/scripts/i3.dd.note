#!/bin/bash

default_note_name="$(date -Idate).org"

note_name="$(zenity --entry --text="Name" --entry-text="${default_note_name}")"
if [[ -z "${note_name}" ]]; then
  note_name="${default_note_name}"
fi

i3.dd.emacs '(find-file "~/notes/'$note_name'")'

# vim: ft=sh

